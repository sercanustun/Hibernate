package com.bilgeadam.libraries;

public class HibernateLibraries {
	
	/*
	 * Sıklıkla kullandığımız annotation
	 * @Entity
	 * @Table
	 * @Id
	 * @GeneratedValue
	 * @Column
	 * @Temporal
	 * @Lob
	 * @Transient
	 * @Version
	 * ...
	 */
	
	/*
	 * persistence:kalıcılık
	 * ORM: object Releating Mapping
	 * Hibernate: hem java hemde .NET
	 */
	
	/*
	 * JPA:Java Persistence API
	 * JPA: Spesification yani yapılar bunun üzerinde inşaa ediliyor.
	 * JPA: tek başına birşey yapamaz bunun yardımcısı olması gerekiyor
	 * örneğin:hibernate
	 * JPA Hibernatetin süper setidir.
	 * JPQL:
	 */
	
	/*
	 * JPA implementation
	 * Hibernate
	 * EclipseLink
	 * Open
	 * DataNucleus
	 */
	
	/*
	 * Transaction:
	 * getTransaction().begin()
	 * kodlar
	 * getTransaction().commit()
	 * Create +
	 * Delete +
	 * Update +
	 * select - veya +
	 */
	
	/*
	 * CRUD
	 * Create persist
	 * Read
	 * Update merge
	 * Delete remove
	 * find find
	 */
	
	/*
	 * Dev+Ops:Docker+Jenkins
	 * Virtual Box VM
	 * Eclipse Database erişmek
	 * JWT
	 */
}
